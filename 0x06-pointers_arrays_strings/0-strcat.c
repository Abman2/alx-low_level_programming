#include "main.h"
/**
* _strcat - function
* @dest: parameter
* @src: parameter
* Return: dest
*/
char *_strcat(char *dest, char *src)
	{
		int i, j, m, k;

		for (i = 0; src[i] != '\0' ; i++)
		{
		}
		for (j = 0; dest[j] != '\0' ; j++)
		{
		}
		total = i + j;
		for (k = i; k < total; k++)
		{
		}
		dest[k] += src[m];

		return (dest);







	}

